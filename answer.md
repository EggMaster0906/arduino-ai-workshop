# Arduino × 生成式 AI 協作開發體驗課｜題目解答

本文件依據 `content/courses/arduino-ai-intro/course.ts` 整理所有「理解檢核」題目。

> 填空題目前採**關鍵字包含**判定：學生答案只要包含列出的任一關鍵字即可通過（`caseSensitive: true` 的題目仍須區分大小寫）。反思題沒有唯一標準答案；下列提供符合教學目標的示例與應涵蓋重點。

## 第一章：Arduino 基礎程式設計

### 1-0｜認識 Arduino

**題目（`1-0-ipo`）：** 在「光敏電阻 → Arduino → Servo」中，Servo 最接近哪一種角色？

**答案：** Output：做出動作。

**說明：** Servo 接收 Arduino 指令並轉動，是輸出裝置。

### 1-1｜程式怎麼運作？

**題目（`1-1-delay`）：** 將 `delay(1000)` 改成 `delay(100)` 後，LED 會怎麼變化？

**答案：** 閃得更快。

**說明：** 等待時間由 1000 毫秒縮短成 100 毫秒，HIGH 與 LOW 的切換更快。

### 1-2｜用變數讓程式更好改

**題目（`1-2-variable`）：** 補上變數名稱：`int ________ = 500;`（用來控制等待時間）

**答案／關鍵字：** `waitTime`（大小寫必須相同）。

**題目（`1-2-predict`）：** 如果 `waitTime` 從 500 改成 1000，你預測 LED 會如何改變？

**示例答案：** LED 會閃得更慢，因為亮與暗各等待的時間都從 500 毫秒增加到 1000 毫秒。

**應包含重點：** 等待時間變長、LED 切換／閃爍變慢。

### 1-3｜Arduino 怎麼感覺外面的世界？

**題目（`1-3-input`）：** `analogRead(A0)` 最主要是在做什麼？

**答案：** 讀取 A0 的類比數值。

**說明：** 它是讀取感測器資料的 Input，尚未控制輸出裝置。

### 1-4｜看見感測器數值

**題目（`1-4-serial`）：** 補上啟動 Serial Monitor 的程式：`Serial.begin(____);`

**答案／關鍵字：** `9600`。

**題目（`1-4-observe`）：** 遮住光敏電阻與照到光時，你觀察到數值如何改變？

**示例答案：** 遮住時讀值約為 ___，照光時約為 ___；兩種情況的數值明顯不同。

**應包含重點：** 寫出遮住與照光兩種觀察值或至少清楚描述數值會改變。數值變大或變小取決於實際分壓接線，因此兩種方向都可能正確。

### 1-5｜把一種數值變成另一種

**題目（`1-5-map`）：** `map(lightValue, 0, 1023, 0, 180)` 的目的為何？

**答案：** 把光線讀值轉成 Servo 可以使用的角度範圍。

**說明：** `map` 是將 Input 的 0–1023 範圍轉換為 Output 所需 0–180 範圍的 Process。

**題目（`1-5-middle`）：** 當 `lightValue` 接近 512 時，`angle` 約接近幾度？

**答案／關鍵字：** `90`、`90度` 或 `90°`。

### 1-6｜讓 Arduino 控制現實世界

**題目（`1-6-write`）：** `motor.write(90)` 會做什麼？

**答案：** 讓 `motor` 嘗試移動到 90°。

**說明：** `write` 的參數代表 Servo 的目標角度。

### 第一章 Final｜光控 Servo

**題目（`1-final-input`）：** 哪個函式負責 Input（讀取光線）？

**答案／關鍵字：** `analogRead`、`analogRead()` 或 `analogRead(A0)`。

**題目（`1-final-process`）：** 哪個函式負責把 `lightValue` 轉換成 `angle`？

**答案／關鍵字：** `map` 或 `map()`。

**題目（`1-final-output`）：** 哪一行負責讓 Servo 實際動作？

**答案／關鍵字：** `motor.write(angle)` 或 `motor.write(angle);`。

**題目（`1-final-reflect`）：** 用自己的話描述這份程式的 Input、Process、Output 各是什麼。

**示例答案：** Input 是用 `analogRead(A0)` 讀取光敏電阻的光線數值；Process 是用 `map` 把讀值換成 0 到 180 的角度；Output 是用 `motor.write(angle)` 控制 Servo 轉到該角度。

**應包含重點：** Input＝讀取光線、Process＝範圍／角度轉換、Output＝控制 Servo。

## 第二章：生成式 AI 協作開發

### 2-1｜為什麼 AI 需要清楚的需求？

**題目（`2-1-context`）：** 哪一個 Prompt 較容易讓 AI 寫出符合需求的程式？

**答案：** Prompt B。

**說明：** Prompt B 說明了硬體、Servo 腳位、輸入、動作規則與請求的協助。

**題目（`2-1-reason`）：** 寫出 Prompt B 比 A 多提供的一項重要資訊，以及它為什麼有用。

**示例答案：** Prompt B 說明 Servo 訊號接在 D9，AI 因此不用猜接線腳位。

**可接受重點：** 硬體（Arduino UNO、SG90）、腳位（D9）、控制輸入（OPEN／CLOSE）、動作邏輯（90°／0°），以及該資訊如何避免 AI 猜測。

### 2-2｜五問需求拆解法

**題目（`2-2-logic`）：** 「OPEN 時 Servo 到 90°；CLOSE 時回到 0°」最屬於五問中的哪一項？

**答案：** 遇到不同情況，要怎麼做？（Logic）。

**說明：** 此敘述定義不同輸入情況下的作品行為。

### 2-3｜先問清楚，再請 AI 幫忙

**題目（`2-3-role`）：** Prompt Coach 發現缺少 Servo 腳位時，最合適的做法是？

**答案：** 詢問學生實際接在哪一腳。

**說明：** 接線是實際事實，AI 不應自行猜測。

### 2-4｜把缺少的事實補齊

**題目（`2-4-clarify`）：** 若你的 Servo 訊號線真的接在 D9，請填入：Servo 訊號線接在 ____。

**答案／關鍵字：** `D9`（不區分大小寫）。

**注意：** 正式操作必須依實際接線填寫；D9 僅是本題範例。

### 2-5｜產生完整 Prompt

**題目（`2-5-preview`）：** 在把 Prompt 送給 Coding AI 前，最重要要檢查什麼？

**答案：** 硬體、腳位、控制邏輯是否是自己的真實需求。

**說明：** Prompt 的重點是事實正確、行為清楚，不是單純字數多。

### 2-6｜取得 AI 程式後，還要做什麼？

**題目（`2-6-test`）：** AI 產生程式後，下一個最合適的步驟是？

**答案：** 閱讀、Upload，並實機測試。

**說明：** AI 產生的程式必須經過實機驗證，不能直接假設正確。

### 2-7｜用事實和 AI 一起 Debug

**題目（`2-7-debug`）：** 若 Servo 不動，你會先提供給 Debug AI 的兩項資訊是什麼？

**示例答案：** 我會提供 Servo 完全不動的實際現象，以及 Servo 的 VCC、GND、訊號線和 Arduino 腳位接法。

**可接受重點：** 從「實際現象、接線、錯誤訊息、已嘗試事項」中提供至少兩項，且描述具體可重現。

### 第二章 Final｜智慧遮光板

**題目（`2-final-ipo`）：** 智慧遮光板中，哪一個組合正確描述 Input → Process → Output？

**答案：** `analogRead` 光線 → `map` 成角度 → `motor.write` 控制 Servo。

**題目（`2-final-reflect`）：** 這次 AI 協作中，你做了哪一個人類不可省略的判斷或測試？為什麼？

**示例答案：** 我先用 Serial Monitor 確認亮與暗時 `lightValue` 的變化方向，再決定 `map` 的角度方向；因為不同接線可能讓數值變大或變小，AI 無法憑空知道實際情況。

**可接受重點：** 確認接線、決定亮暗方向、Upload 實測或記錄 Debug 現象其中一項，以及說明為何不能交由 AI 猜測。

## 補充：Prompt Builder 活動的參考答案

以下為活動填寫引導，並非有唯一答案的自動評分題。硬體與腳位必須依學生實際使用的器材填寫。

### Serial 控制小柵欄

- **目標：** 使用 Arduino UNO 與 SG90 製作可開關的小柵欄。
- **硬體：** Arduino UNO、SG90 Servo、電腦與 Arduino IDE。
- **控制方式：** 從 Serial Monitor 輸入 `OPEN` 或 `CLOSE`。
- **控制邏輯：** `OPEN` 時 Servo 移到 90°；`CLOSE` 時 Servo 回到 0°。
- **Servo 訊號腳位：** 依實際接線填寫，例如 `D9`。
- **AI 協助：** 例如「寫完整程式，並逐段解釋」。

### 智慧遮光板

- **目標：** 環境越亮遮光板越關閉，環境越暗越打開。
- **硬體：** Arduino UNO、光敏電阻與固定電阻（分壓電路）、SG90 Servo。
- **輸入與控制：** 從 A0 使用 `analogRead` 讀取光敏電阻的類比數值。
- **控制邏輯：** 先實測亮暗時讀值方向，再用 `map` 將 0–1023 對應到 Servo 的 0–180 或 180–0 度；方向必須配合實際接線與預期的開／關動作。
- **Servo 訊號腳位：** 依實際接線填寫，例如 `D9`。
- **AI 協助：** 例如「寫完整程式，並逐段解釋」。
